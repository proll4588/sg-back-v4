type Group {
  id: Int!
  title: String!
}

type Student {
  passbookNumber: Int!
  name: String!
  Group: Group!
}

type Role {
  id: Int!
  title: String!
}

type User {
  id: Int!
  login: String!
  Role: Role!
  Student: Student
}
